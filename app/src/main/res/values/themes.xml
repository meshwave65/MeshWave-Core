<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      Tema base do aplicativo.
      - Pai: Theme.Material3.DayNight.NoActionBar.
        - "Material3" é o sistema de design moderno usado com o Compose.
        - "DayNight" adapta-se automaticamente aos modos claro e escuro.
        - "NoActionBar" é crucial para apps Compose, pois o Compose gerencia a própria UI, incluindo a barra superior.
    -->
    <style name="Theme.MeshWaveCore" parent="Theme.Material3.DayNight.NoActionBar">
        <!--
          Você pode customizar cores aqui, mas com o Compose,
          é mais comum definir as cores diretamente no código Kotlin do tema do Compose.
          Por enquanto, vamos deixar o tema XML o mais simples possível para resolver o erro de build.
        -->
    </style>
</resources>
