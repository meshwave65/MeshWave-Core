package com.meshwave.core

import android.content.Context

/**
 * MÓDULO LEGADO - TEMPORARIAMENTE NEUTRALIZADO.
 * O conteúdo foi comentado para permitir a compilação.
 * A lógica de Wi-Fi Direct será reimplementada em um novo Controller.
 */
class WiFiDirectModule(private val context: Context) {
    // Todo o código antigo foi removido/comentado.
    fun somePlaceholderFunction() {
        // Função vazia para manter o arquivo válido
    }
}