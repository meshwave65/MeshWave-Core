package com.meshwave.core

import android.os.Bundle
import android.view.LayoutInflater
import android.view.View
import android.view.ViewGroup
import androidx.fragment.app.Fragment

/**
 * FRAGMENT LEGADO - TEMPORARIAMENTE NEUTRALIZADO.
 * O conteúdo foi comentado para permitir a compilação.
 * Será removido ou refatorado para Jetpack Compose posteriormente.
 */
class StatusFragment : Fragment() {
    override fun onCreateView(inflater: LayoutInflater, container: ViewGroup?, savedInstanceState: Bundle?): View? {
        // Retorna uma view vazia para não quebrar o app
        return View(context)
    }
    // Todo o código antigo foi removido/comentado.
}
